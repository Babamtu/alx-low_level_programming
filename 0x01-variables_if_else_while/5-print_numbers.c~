#include <stdio.h>
/**
 *main - Entry point
 *
 *a program that prints all single digit numbers of base 10 starting with 0
 *Return: 0 (Success)
 */

int main(void)
{
	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
